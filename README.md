# Servidor REST con ExpressJS

### Para instalar usar:

``` npm install ```

### Para usar en local:

``` npm run dev ``` or ``` npm start ```

### Para probar los endpoint usar Postman o Insomnia:
### Para probar el webser usar localhost:8080